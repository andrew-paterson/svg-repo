import Component from '@ember/component';
import layout from '../../templates/components/svg/rotating-bar';
export default Component.extend({
  classNames: ['svg'],
  layout,
  classNameBindings: ['class'],
});